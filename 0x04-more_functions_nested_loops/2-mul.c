#include "main.h"

/**
 * mul - prints the multiplication of two numbers
 * @a: first value
 * @b: second value
 * Return: returns the product of the two values
 */

int mul(int a, int b)
{
	return (a * b);
}
